#ifndef ALIGN_HPP
    #define ALIGN_HPP

    enum class Align {
        Horizontal,
        Vertical,
    };

    enum class TextAlignment {
        Left,
        Right,
        Center,
    };

    enum class HorizontalAlignment {
        Left,
        Right,
        Center,
    };

    enum class VerticalAlignment {
        Top,
        Bottom,
        Center,
    };
#endif